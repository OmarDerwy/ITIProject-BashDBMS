#! /usr/bin/bash

source ./createFunc
source ./useFunc

shopt -s extglob
read -p "> " -a input

case ${input[0]} in
+([cC][rR][eE][aA][tT][eE]))
    createFunc ${input[@]:1}
    ;;
+([uU][sS][eE]))
    useFuncSQL ${input[@]:1}
    ;;
esac

echo $databaseInUse