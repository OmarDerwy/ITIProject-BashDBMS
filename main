#! /usr/bin/bash

source ./createFunc
source ./useFunc

shopt -s extglob
read -p "> " -a input

case ${input[0]} in
+([createCREATE]))
    createFunc ${input[@]:1}
    ;;
+([useUSE]))
    useFuncSQL ${input[@]:1}
    ;;
esac

echo $databaseInUse