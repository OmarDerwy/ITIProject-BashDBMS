# !usr/bin/bash
shopt -s extglob

listDatabasesFuncSQL(){
    if [[ $# -ne 1 ]]
    then 
        echo Invalid number of arguments.
    else
            ls Databases
    fi
}

listDatabasesFunc(){
    ls Databases;
}
