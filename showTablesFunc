# !usr/bin/bash
shopt -s extglob

showTablesFuncSQL(){
    if [[ $# -ne 1 ]]
    then 
        echo Invalid number of arguments.
    else
        ls | grep -v '_metadata$'
    fi
}

showTablesFunc(){
    ls | grep -v '_metadata$'

}
